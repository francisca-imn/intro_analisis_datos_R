# Introducción al Análisis de Datos con R  

Este repositorio contiene el material del curso **Introducción al Análisis de Datos**, dictado en el contexto de la empresa **Cienciambiental**, con énfasis en el uso del lenguaje **R** para el análisis, manipulación y visualización de datos.

El curso está orientado a entregar herramientas básicas para trabajar con datos de manera ordenada, reproducible y eficiente, utilizando principalmente el ecosistema **tidyverse**.

---

## 📘 Contenido del curso

El archivo principal del curso es:

- **`curso_programacion_r`**  
  Contiene los apuntes completos del curso, incluyendo ejemplos, explicaciones y código trabajado durante las clases.

El curso se estructura en **7 clases**, que abordan progresivamente los siguientes contenidos:

### 1️⃣ Introducción al tidyverse  
Manipulación básica de datos utilizando funciones fundamentales como:

- `filter()`
- `select()`
- `arrange()`
- `mutate()`
- `rename()`
- `slice()`
- `n()`, `n_distinct()`
- `count()`

---

### 2️⃣ Uso del pipe `%>%`  
Introducción al operador *pipe* para encadenar operaciones y construir flujos de trabajo más legibles y eficientes.

---

### 3️⃣ Agrupación y resumen de datos  
Trabajo con datos agrupados mediante:

- `group_by()`
- `summarise()`

---

### 4️⃣ Unión y reestructuración de tablas  
Manejo de múltiples fuentes de datos y reordenamiento de tablas mediante:

- Uniones:  
  - `left_join()`  
  - `right_join()`  
  - `inner_join()`  
  - `full_join()`
- Reestructuración de datos:  
  - `pivot_longer()`  
  - `pivot_wider()`

---

### 5️⃣ Condicionales  
Creación de variables y lógica condicional usando:

- `ifelse()`
- `case_when()`

---

### 6️⃣ Visualización de datos  
Introducción a la visualización con **ggplot2**, incluyendo buenas prácticas, ejemplos comunes y tips para mejorar la claridad de gráficos.

---

### 7️⃣ Reportes reproducibles  
Generación de reportes a partir de:

- **Quarto**
- **R Markdown**

con énfasis en la reproducibilidad y comunicación de resultados.

---

## 📝 Tareas y evaluaciones

Para aprobar el curso se deben realizar distintas **tareas prácticas**. Por motivos de **privacidad**, en este repositorio **no se incluyen los enunciados originales** de las tareas. Únicamente se encuentran disponibles los **archivos correspondientes a mis resoluciones personales**, con fines de respaldo y seguimiento del aprendizaje.

---

## 📂 Estructura general del repositorio

- `curso_programacion_r.*` → Apuntes principales del curso  
- `clase_01/` → Ejemplo excepcional de la clase 1.
- `tareas/` → Resoluciones personales de las tareas  
- `datos/` → Datos utilizados durante el curso  